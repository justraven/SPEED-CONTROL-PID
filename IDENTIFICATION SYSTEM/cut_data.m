% % CUT DATA FROM NOISE
% fs = 20; %20Hz of sampling, one data per 50ms
% 
% remove_seconds = 3;
% 
% remove_points = remove_seconds * fs;
% 
% DATASET_1_INPUT_REV = DATASET_1_INPUT(remove_points+1:end);
% DATASET_1_OUTPUT_REV = DATASET_1_OUTPUT(remove_points+1:end);